# Consumed by verify-l1es.sh Check 6 (decode roundtrip)
FIX_INDEX="l1es_verify_enc"
FIX_QUERY="Accounting service started"
# Token that exists ONLY in decoded output (not in the stored ~hash,val encoding
# nor in the query text) — finding it in the search response proves decode ran.
FIX_PROOF="stdout"
